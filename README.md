# Graph Query System
This system uses a language model to translate text into graph and database queries.

## Backend API
The `/query` endpoint takes a POST request with natural language.
The `/graph` endpoint takes a GET request to supply initial node links.
